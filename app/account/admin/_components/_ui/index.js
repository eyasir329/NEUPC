/**
 * @file Admin panel _ui — re-exports the shared dashboard primitives
 *   from app/account/_components/ui/dashboard.js so admin screens use
 *   the same visual language as member, advisor, and executive panels.
 */

export {
  PageShell,
  PageHeader,
  GlassCard,
  SectionHeader,
  IconChip,
  StatCard,
  Pill,
  GradientBar,
  TabBar,
  EmptyState,
  ActionButton,
  Avatar,
  Sparkline,
  StaggerList,
} from '../../../_components/ui/dashboard';
