/**
 * @file Blogs loading page — skeleton UI shown while the
 *   blogs page data resolves.
 *
 * @module AdminBlogsLoading
 */

import AccountLoading from '../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="cards" title="Blogs" />;
}
