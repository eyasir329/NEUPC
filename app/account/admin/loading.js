/**
 * @file Admin Dashboard loading page — skeleton UI shown while the
 *   admin dashboard page data resolves.
 *
 * @module AdminDashboardLoading
 */

import AccountLoading from '../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="dashboard" title="Admin Dashboard" />;
}
