/**
 * @file Approvals loading page — skeleton UI shown while the
 *   approvals page data resolves.
 *
 * @module AdvisorApprovalsLoading
 */

import AccountLoading from '../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="table" title="Approvals" />;
}
