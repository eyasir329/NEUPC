/**
 * @file Events loading page — skeleton UI shown while the
 *   events page data resolves.
 *
 * @module AdvisorEventsLoading
 */

import AccountLoading from '../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="cards" title="Events" />;
}
