/**
 * @file Manage Blogs loading page — skeleton UI shown while the
 *   manage blogs page data resolves.
 *
 * @module ExecutiveManageBlogsLoading
 */

import AccountLoading from '../../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="cards" title="Manage Blogs" />;
}
