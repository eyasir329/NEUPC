/**
 * @file Generate Certificates loading page — skeleton UI shown while the
 *   generate certificates page data resolves.
 *
 * @module ExecutiveGenerateCertificatesLoading
 */

import AccountLoading from '../../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="form" title="Generate Certificates" />;
}
