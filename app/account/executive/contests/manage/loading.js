/**
 * @file Manage Contests loading page — skeleton UI shown while the
 *   manage contests page data resolves.
 *
 * @module ExecutiveManageContestsLoading
 */

import AccountLoading from '../../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="cards" title="Manage Contests" />;
}
