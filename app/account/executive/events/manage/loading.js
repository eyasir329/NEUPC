/**
 * @file Manage Events loading page — skeleton UI shown while the
 *   manage events page data resolves.
 *
 * @module ExecutiveManageEventsLoading
 */

import AccountLoading from '../../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="cards" title="Manage Events" />;
}
