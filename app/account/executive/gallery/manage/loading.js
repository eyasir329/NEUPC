/**
 * @file Manage Gallery loading page — skeleton UI shown while the
 *   manage gallery page data resolves.
 *
 * @module ExecutiveManageGalleryLoading
 */

import AccountLoading from '../../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="cards" title="Manage Gallery" />;
}
