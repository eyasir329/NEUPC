/**
 * @file Create Notice loading page — skeleton UI shown while the
 *   create notice page data resolves.
 *
 * @module ExecutiveCreateNoticeLoading
 */

import AccountLoading from '../../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="form" title="Create Notice" />;
}
