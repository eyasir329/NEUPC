/**
 * @file Registrations loading page — skeleton UI shown while the
 *   registrations page data resolves.
 *
 * @module ExecutiveRegistrationsLoading
 */

import AccountLoading from '../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="table" title="Registrations" />;
}
