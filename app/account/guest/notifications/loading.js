/**
 * @file Notifications loading page — skeleton UI shown while the
 *   notifications page data resolves.
 *
 * @module GuestNotificationsLoading
 */

import AccountLoading from '../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="table" title="Notifications" />;
}
