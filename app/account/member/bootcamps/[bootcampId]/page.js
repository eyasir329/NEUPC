// Layout at this segment mounts BootcampLearningClient and handles auth/data.
// This page component is never rendered — layout renders the shell directly.
export default function BootcampPage() {
  return null;
}
