/**
 * @file Problem Set loading page — skeleton UI shown while the
 *   problem set page data resolves.
 *
 * @module MemberProblemSetLoading
 */

import AccountLoading from '../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="cards" title="Bootcamps" />;
}
