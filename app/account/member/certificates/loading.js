/**
 * @file Certificates loading page — skeleton UI shown while the
 *   certificates page data resolves.
 *
 * @module MemberCertificatesLoading
 */

import AccountLoading from '../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="cards" title="Certificates" />;
}
