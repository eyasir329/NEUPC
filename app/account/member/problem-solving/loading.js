/**
 * @file Contests loading page — skeleton UI shown while the
 *   contests page data resolves.
 *
 * @module MemberContestsLoading
 */

import AccountLoading from '../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="cards" title="Problem Solving" />;
}
