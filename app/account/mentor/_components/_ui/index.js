'use client';

import Link from 'next/link';
import { motion } from 'framer-motion';

// Re-export everything from member _ui so mentor pages share the same design system
export {
  PageShell,
  PageHeader,
  GlassCard,
  SectionHeader,
  IconChip,
  StatCard,
  Pill,
  GradientBar,
  TabBar,
  EmptyState,
  ActionButton,
  Avatar,
  Sparkline,
  StaggerList,
} from '@/app/account/member/_components/_ui';
