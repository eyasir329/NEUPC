/**
 * @file Assigned Members loading page — skeleton UI shown while the
 *   assigned members page data resolves.
 *
 * @module MentorAssignedMembersLoading
 */

import AccountLoading from '../../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="table" title="Assigned Members" />;
}
