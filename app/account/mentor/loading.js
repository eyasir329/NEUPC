/**
 * @file Mentor Dashboard loading page — skeleton UI shown while the
 *   mentor dashboard page data resolves.
 *
 * @module MentorDashboardLoading
 */

import AccountLoading from '../_components/AccountLoading';

export default function Loading() {
  return <AccountLoading variant="dashboard" title="Mentor Dashboard" />;
}
