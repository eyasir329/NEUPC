# NEUPC Database Visual Guide

Visual representations and diagrams for understanding the NEUPC database structure.

## Table of Contents

1. [Core Entity Relationships](#core-entity-relationships)
2. [Role-Based Access Flow](#role-based-access-flow)
3. [Event Workflow](#event-workflow)
4. [Content Management Flow](#content-management-flow)
5. [Mentorship System](#mentorship-system)
6. [Data Flow Diagrams](#data-flow-diagrams)

---

## Core Entity Relationships

### Authentication & User Management

```
┌──────────────────────────────────────────────────────────┐
│                     AUTHENTICATION                        │
└──────────────────────────────────────────────────────────┘

         ┌─────────────┐
         │    USERS    │ (id, email, password_hash, full_name)
         └──────┬──────┘
                │
                │ 1:N
                ▼
         ┌─────────────┐
         │ USER_ROLES  │ (user_id, role_id, expires_at)
         └──────┬──────┘
                │ N:1
                ▼
         ┌─────────────┐
         │    ROLES    │ (id, name, priority)
         └──────┬──────┘
                │ 1:N        ┌──────────────────┐
                └───────────▶│ ROLE_PERMISSIONS │
                             └──────┬───────────┘
                                    │ N:1
                                    ▼
                             ┌──────────────────┐
                             │   PERMISSIONS    │
                             └──────────────────┘

Roles: guest → member → mentor → executive → advisor → admin
                (1)      (2)      (3)         (4)        (5)      (6)
```

### Member Profiles & Statistics

```
┌──────────────────────────────────────────────────────────┐
│                     MEMBER DATA                           │
└──────────────────────────────────────────────────────────┘

    ┌─────────────┐
    │    USERS    │
    └──────┬──────┘
           │ 1:1
           ▼
    ┌─────────────────────┐
    │  MEMBER_PROFILES    │ (student_id, batch, department,
    └──────┬──────────────┘  codeforces_handle, skills[])
           │ 1:1
           ▼
    ┌─────────────────────┐
    │ MEMBER_STATISTICS   │ (codeforces_rating, problems_solved,
    └─────────────────────┘  total_contests, last_sync_at)
```

### Committee Structure

```
┌──────────────────────────────────────────────────────────┐
│                     COMMITTEE                             │
└──────────────────────────────────────────────────────────┘

    ┌─────────────────────┐
    │COMMITTEE_POSITIONS  │ (title, category, display_order)
    └──────┬──────────────┘  President, VP, Dev Lead...
           │ 1:N
           ▼
    ┌─────────────────────┐
    │ COMMITTEE_MEMBERS   │◀──┐
    └─────────────────────┘   │ N:1
           │                  │
           │ N:1              │
           └─────────────▶┌──────┐
                          │USERS │
                          └──────┘
```

---

## Role-Based Access Flow

### Permission Check Flow

```
User Request
     │
     ▼
┌─────────────┐
│ Get User ID │
└─────┬───────┘
      │
      ▼
┌──────────────────┐       ┌────────────────┐
│ Fetch User Roles │──────▶│ Get Role Names │
└──────────────────┘       └────────┬───────┘
                                    │
                                    ▼
                           ┌─────────────────┐
                           │ Check Permission│
                           └────────┬────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    ▼                               ▼
              ┌──────────┐                   ┌──────────┐
              │ ALLOWED  │                   │ DENIED   │
              └──────────┘                   └──────────┘
```

### Role Hierarchy

```
                    ┌─────────┐
                    │  ADMIN  │ (Priority 6)
                    └────┬────┘
                         │ Can manage all
                         ▼
                    ┌─────────┐
                    │ ADVISOR │ (Priority 5)
                    └────┬────┘
                         │ Can approve/oversee
                         ▼
                  ┌──────────────┐
                  │  EXECUTIVE   │ (Priority 4)
                  └──────┬───────┘
                         │ Can manage content/events
                         ▼
                    ┌─────────┐
                    │ MENTOR  │ (Priority 3)
                    └────┬────┘
                         │ Can guide members
                         ▼
                    ┌─────────┐
                    │ MEMBER  │ (Priority 2)
                    └────┬────┘
                         │ Authenticated access
                         ▼
                    ┌─────────┐
                    │  GUEST  │ (Priority 1)
                    └─────────┘
                         │ Public access only
```

---

## Event Workflow

### Event Lifecycle

```
┌──────────────────────────────────────────────────────────┐
│                     EVENT MANAGEMENT                      │
└──────────────────────────────────────────────────────────┘

1. CREATE (Executive/Admin)
   ┌─────────────┐
   │   EVENTS    │ status: 'draft'
   └──────┬──────┘
          │
          ▼
2. APPROVAL (Advisor) - Optional
   ┌─────────────┐
   │   EVENTS    │ status: 'upcoming', approved_by set
   └──────┬──────┘
          │
          ▼
3. REGISTRATION OPENS
   ┌──────────────────────┐
   │ EVENT_REGISTRATIONS  │◀─── Members register
   └──────┬───────────────┘
          │
          ▼
4. EVENT OCCURS
   ┌─────────────┐
   │   EVENTS    │ status: 'ongoing'
   └──────┬──────┘
          │
          ▼
5. MARK ATTENDANCE
   ┌──────────────────────┐
   │ EVENT_REGISTRATIONS  │ attended: true
   └──────┬───────────────┘
          │
          ▼
6. UPLOAD PHOTOS
   ┌──────────────┐
   │EVENT_GALLERY │ Multiple photos/videos
   └──────┬───────┘
          │
          ▼
7. COMPLETE
   ┌─────────────┐
   │   EVENTS    │ status: 'completed'
   └─────────────┘
          │
          ▼
8. ISSUE CERTIFICATES (Optional)
   ┌──────────────┐
   │ CERTIFICATES │ Linked to event, participants
   └──────────────┘
```

### Event Registration Flow

```
        MEMBER                          SYSTEM
          │                               │
          │   Click "Register"            │
          ├──────────────────────────────▶│
          │                               │
          │                         Check capacity
          │                         Check deadline
          │                               │
          │◀──────────────────────────────┤
          │   Registration Form           │
          │                               │
          │   Submit Form Data            │
          ├──────────────────────────────▶│
          │                               │
          │                    Create registration
          │                    Send confirmation email
          │                    Create notification
          │                               │
          │◀──────────────────────────────┤
          │   Success Message             │
```

---

## Content Management Flow

### Blog Post Workflow

```
┌──────────────────────────────────────────────────────────┐
│                     BLOG MANAGEMENT                       │
└──────────────────────────────────────────────────────────┘

DRAFT
  │
  │ Author creates post
  ▼
┌─────────────┐
│ BLOG_POSTS  │ status: 'draft'
└──────┬──────┘
       │
       │ Executive/Admin publishes
       ▼
┌─────────────┐
│ BLOG_POSTS  │ status: 'published', published_at set
└──────┬──────┘
       │
       │ Visible to public
       │
       ├─────────────────┐
       │                 │
       ▼                 ▼
┌──────────────┐   ┌──────────┐
│ VIEW COUNTER │   │ COMMENTS │
└──────────────┘   └────┬─────┘
                        │
                        ├─────────────┐
                        │             │
                        ▼             ▼
                  ┌──────────┐  ┌──────────┐
                  │  REPLY   │  │  REPLY   │ (nested)
                  └──────────┘  └──────────┘
```

### Content Types

```
┌─────────────────────────────────────────────────┐
│              CONTENT ECOSYSTEM                   │
└─────────────────────────────────────────────────┘

┌──────────────┐
│  BLOG POSTS  │ Technical articles, tutorials
└──────────────┘

┌──────────────┐
│   ROADMAPS   │ Learning paths (JSONB structure)
└──────────────┘

┌──────────────┐
│  RESOURCES   │ Curated links to courses, books, tools
└──────────────┘

┌──────────────┐
│   NOTICES    │ Announcements (with expiry, priority)
└──────────────┘

┌──────────────┐
│   GALLERY    │ Photos/Videos (event_id optional)
└──────────────┘
```

---

## Mentorship System

### Mentorship Workflow

```
┌──────────────────────────────────────────────────────────┐
│                  MENTORSHIP PROGRAM                       │
└──────────────────────────────────────────────────────────┘

1. ASSIGNMENT
      ADMIN/EXECUTIVE
            │
            ▼
      ┌──────────────┐
      │ MENTORSHIPS  │ (mentor_id, mentee_id, focus_area)
      └──────┬───────┘
             │
             │ status: 'active'
             ▼

2. SESSIONS
      MENTOR schedules
            │
            ▼
      ┌─────────────────────┐
      │ MENTORSHIP_SESSIONS │ (date, duration, topic, notes)
      └─────────────────────┘
            │
            │ After session: feedback
            ▼

3. TRACKING
      MENTOR updates
            │
            ▼
      ┌─────────────────┐
      │ MEMBER_PROGRESS │ (period, problems_solved, 
      └─────────────────┘  contests, rating_change)
            │
            │ Weekly/Monthly
            ▼

4. TASKS
      MENTOR assigns
            │
            ▼
      ┌──────────────┐
      │ WEEKLY_TASKS │ (problems[], deadline)
      └──────┬───────┘
             │
             ▼
      MENTEE submits
             │
             ▼
      ┌──────────────────┐
      │ TASK_SUBMISSIONS │ (code, status, feedback)
      └──────────────────┘
```

### Mentorship Relationships

```
     ┌────────────┐
     │   MENTOR   │ (User with mentor role)
     └─────┬──────┘
           │ 1:N
           ▼
     ┌────────────┐
     │MENTORSHIPS │
     └─────┬──────┘
           │ N:1
           ▼
     ┌────────────┐
     │  MENTEE    │ (User with member role)
     └────────────┘

One mentor can have multiple mentees
One mentee can have one active mentor at a time
```

---

## Data Flow Diagrams

### User Registration Flow

```
NEW USER
   │
   │ Fills join form
   ▼
┌─────────────┐
│JOIN_REQUESTS│ status: 'pending'
└──────┬──────┘
       │
       │ EXECUTIVE/ADMIN reviews
       ├──────────────┬───────────────┐
       │              │               │
       ▼              ▼               ▼
   APPROVED       REJECTED       PENDING
       │              │
       │              │ Set rejection_reason
       │              ▼
       │         ┌──────────────┐
       │         │JOIN_REQUESTS │ status: 'rejected'
       │         └──────────────┘
       │
       │ Create user account
       ▼
   ┌────────┐
   │ USERS  │ email_verified: false
   └───┬────┘
       │
       │ Send verification email
       │
       │ User clicks link
       ▼
   ┌────────┐
   │ USERS  │ email_verified: true
   └───┬────┘
       │
       │ Auto-assign member role
       ▼
   ┌────────────┐
   │ USER_ROLES │ (user_id, member_role_id)
   └─────┬──────┘
         │
         │ Create profile
         ▼
   ┌─────────────────┐
   │ MEMBER_PROFILES │ (student_id, batch, department)
   └─────────────────┘
```

### Achievement Recording Flow

```
COMPETITION RESULT
        │
        │ EXECUTIVE/ADMIN inputs
        ▼
  ┌──────────────┐
  │ ACHIEVEMENTS │ (title, contest, result, year)
  └──────┬───────┘
         │
         │ Link participants
         ▼
  ┌───────────────────┐
  │MEMBER_ACHIEVEMENTS│ (achievement_id, user_id)
  └─────────┬─────────┘
            │
            │ Generate certificates
            ▼
        ┌──────────────┐
        │ CERTIFICATES │ (achievement_id, recipient_id,
        └──────────────┘  certificate_number, pdf_url)
            │
            │ Send notifications
            ▼
     ┌──────────────────┐
     │  NOTIFICATIONS   │ "New achievement recorded!"
     └──────────────────┘
```

### Contest Participation Flow

```
CONTEST SCHEDULED
       │
       │ ADMIN creates
       ▼
  ┌──────────┐
  │ CONTESTS │ status: 'upcoming'
  └─────┬────┘
        │
        │ MEMBERS register
        ▼
  ┌───────────────────────┐
  │ CONTEST_PARTICIPANTS  │ (contest_id, user_id)
  └──────────┬────────────┘
             │
             │ Contest time arrives
             ▼
        ┌──────────┐
        │ CONTESTS │ status: 'running'
        └─────┬────┘
              │
              │ Contest ends
              ▼
        ┌──────────┐
        │ CONTESTS │ status: 'finished'
        └─────┬────┘
              │
              │ ADMIN updates results
              ▼
        ┌───────────────────────┐
        │ CONTEST_PARTICIPANTS  │ (rank, score, problems_solved)
        └───────────────────────┘
              │
              │ Auto-update statistics
              ▼
        ┌──────────────────┐
        │MEMBER_STATISTICS │ (total_contests++)
        └──────────────────┘
```

### Discussion Flow

```
MEMBER
  │
  │ Creates thread
  ▼
┌────────────────────┐
│ DISCUSSION_THREADS │ (category_id, title, content)
└─────────┬──────────┘
          │
          │ OTHER MEMBERS reply
          ▼
┌────────────────────┐
│ DISCUSSION_REPLIES │ (thread_id, content)
└─────────┬──────────┘
          │
          ├───────────────┬─────────────┐
          │               │             │
          ▼               ▼             ▼
    NESTED REPLY    UPVOTE       MARK AS SOLUTION
          │               │             │
          ▼               ▼             ▼
┌──────────────────┐ ┌──────────┐ ┌──────────────┐
│DISCUSSION_REPLIES│ │  VOTES   │ │    THREAD    │
│  (parent_id)     │ │ (vote:+1)│ │(is_solved:T) │
└──────────────────┘ └──────────┘ └──────────────┘
```

---

## Activity Logging

### Audit Trail

```
USER ACTION (Create/Update/Delete)
        │
        ▼
   Auto-trigger
        │
        ▼
┌──────────────┐
│ACTIVITY_LOGS │ (user_id, action, entity_type, entity_id,
└──────────────┘  details_json, ip_address, timestamp)

Examples:
- User login: (user_123, 'login', NULL, NULL, {}, '192.168.1.1')
- Event created: (admin_1, 'create', 'event', event_456, {title: '...'})
- Blog edited: (exec_2, 'update', 'blog', post_789, {changes: ...})
- User deleted: (admin_1, 'delete', 'user', user_999, {email: '...'})
```

---

## Notification System

### Notification Flow

```
TRIGGERING EVENT
       │
       ├────────────────┬─────────────────┬──────────────────┐
       │                │                 │                  │
       ▼                ▼                 ▼                  ▼
 Event Created    Contest Start   New Achievement   Mentioned in
                                                      Discussion
       │                │                 │                  │
       └────────────────┴─────────────────┴──────────────────┘
                              │
                              ▼
                     ┌─────────────────┐
                     │ Create Multiple │
                     │  NOTIFICATIONS  │ (one per target user)
                     └────────┬────────┘
                              │
                    ┌─────────┴──────────┐
                    │                    │
                    ▼                    ▼
             ┌──────────────┐    ┌──────────────┐
             │  In-App      │    │   Email      │ (optional)
             │  Notification│    │   Notification│
             └──────────────┘    └──────────────┘
                    │
                    │ User reads
                    ▼
              ┌─────────────┐
              │NOTIFICATIONS│ is_read: true, read_at: timestamp
              └─────────────┘
```

---

## Analytics & Reporting

### Data Aggregation

```
┌────────────────────────────────────────────────────┐
│             ANALYTICS DASHBOARD                     │
└────────────────────────────────────────────────────┘

Raw Tables          →    Aggregation    →    Views/Reports
─────────────            ────────────         ──────────────

EVENT_REGISTRATIONS  →   Count by event  →  EVENT_STATISTICS
                         Count attended
                         
MEMBER_STATISTICS    →   Rankings        →  LEADERBOARD
                         Averages
                         
CONTEST_PARTICIPANTS →   Performance     →  MEMBER_ANALYTICS
                         Trends
                         
ACTIVITY_LOGS        →   User actions    →  ADMIN_REPORTS
                         Time series
```

---

## Database Health Monitoring

### Key Metrics

```
┌─────────────────────────────────────────────────────────┐
│                 MONITORING POINTS                        │
└─────────────────────────────────────────────────────────┘

1. TABLE SIZES
   ├─ users: ~1K rows
   ├─ member_profiles: ~500 rows
   ├─ events: ~100 rows
   ├─ blog_posts: ~200 rows
   ├─ activity_logs: ~10K rows (grow continuously)
   └─ notifications: ~5K rows

2. QUERY PERFORMANCE
   ├─ Event listing: < 50ms
   ├─ Member dashboard: < 100ms
   ├─ Blog post fetch: < 30ms
   └─ Leaderboard: < 200ms

3. CONNECTION POOL
   ├─ Active connections: 5-20
   ├─ Idle connections: 5-10
   └─ Max connections: 100

4. INDEX USAGE
   ├─ idx_users_email: High usage
   ├─ idx_events_status: High usage
   ├─ idx_blog_posts_published: Medium usage
   └─ idx_notifications_user: High usage
```

---

## Summary

This visual guide provides:

- ✅ Entity relationship diagrams
- ✅ Workflow diagrams for key features
- ✅ Data flow visualizations
- ✅ Role hierarchy
- ✅ System architecture overview

For implementation details, see:

- [Database Schema](./database-schema.sql)
- [Database Documentation](./database-documentation.md)
- [Migration Guide](./database-migration-guide.md)
- [Quick Reference](./database-quick-reference.md)
